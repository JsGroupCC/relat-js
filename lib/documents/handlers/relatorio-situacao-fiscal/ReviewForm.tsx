"use client"

import type { RelatorioSituacaoFiscal } from "./schema"

/**
 * Sprint 1: stub. Substituído por implementação real no Sprint 2 (rev/dashboard).
 */
export function ReviewForm(_props: {
  data: RelatorioSituacaoFiscal
  onChange: (d: RelatorioSituacaoFiscal) => void
}) {
  return (
    <div className="text-sm text-muted-foreground">
      ReviewForm — implementação chega no Sprint 2.
    </div>
  )
}
